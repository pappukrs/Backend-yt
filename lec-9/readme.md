<!-- 
appendFile
   The appendFile function is used to append content to an existing file, adding the new content to the end of the file without modifying the existing content. If the file does not exist, appendFile will create it. Here's an example 
   
   -->

<!-- 
writeFile
      The writeFile function is used to write content to a file, either creating a new file or overwriting the existing file with the new content. If the file already exists, the writeFile function will replace its content entirely.
       -->